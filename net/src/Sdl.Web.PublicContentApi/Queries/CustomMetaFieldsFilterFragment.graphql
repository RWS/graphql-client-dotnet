fragment customMetaFields on Item {
  customMetas(filter: {0}) {
    edges {
      node {
          id
          itemId
          key
          namespaceId
          publicationId
          value
          valueType    
      }
    }
  }
}