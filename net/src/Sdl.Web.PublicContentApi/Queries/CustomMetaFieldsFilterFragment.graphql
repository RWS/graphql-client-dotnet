fragment CustomMetaFields on Item {
  customMetas(filter: @customMetaFilter) {
    edges {
      node {
          id
          itemId
          key
          namespaceId
          publicationId
          value
          valueType    
      }
    }
  }
}