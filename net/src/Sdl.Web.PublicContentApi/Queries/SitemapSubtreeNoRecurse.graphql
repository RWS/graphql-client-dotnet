query sitemapSubtree($namespaceId: Int!, $publicationId: Int!, $taxonomyNodeId: String, $includeAncestors: Boolean) {          
    sitemapSubtree(namespaceId: $namespaceId, publicationId: $publicationId, taxonomyNodeId: $taxonomyNodeId, includeAncestors: $includeAncestors) {
		...TaxonomyItemFields    
    }
}