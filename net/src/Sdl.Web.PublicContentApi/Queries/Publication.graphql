 query publication($namespaceId: Int! $publicationId: Int! $contextData: [InputClaimValue!]) {
	binaryComponent(namespaceId: $namespaceId, publicationId: $publicationId, contextData: $contextData) {
		...ItemFields
		...PublicationFieldsFragment
	}
}