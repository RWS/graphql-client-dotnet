fragment CustomMetaFields on Item {
  customMetas@customMetaArgs {
    edges {
      node {
          id
          itemId
          key
          namespaceId
          publicationId
          value
          valueType    
      }
    }
  }
}