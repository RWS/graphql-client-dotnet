query binaryLink($namespaceId: Int!, $publicationId: Int!, $binaryId: Int!, $variantId: String) {
	binaryLink(namespaceId: $namespaceId, publicationId: $publicationId, binaryId: $binaryId, variantId: $variantId) {
		url
	}
}