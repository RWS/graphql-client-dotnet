fragment RecurseItems on TaxonomySitemapItem {
	...TaxonomyItemFields
    items {		  
		...on TaxonomySitemapItem {
			...TaxonomyItemFields
		}
		...on PageSitemapItem {
			...TaxonomyPageFields
		}
    }
}